import 'package:flutter/material.dart';

class ApplicationSubmitScreen extends StatelessWidget {
  const ApplicationSubmitScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Submit Application')),
      body: const Center(child: Text('Application Submit Under Construction')),
    );
  }
}
